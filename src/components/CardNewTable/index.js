import React, { useState } from 'react';
import { Text } from 'react-native';

import {
    Container,
} from './styles';

export const CardNewTable = ({ name, price }) => {

    return (
        <Container>
            <Text>ADD</Text>
        </Container>
    );
}